/*
```javascript
*/
// Define the fixed stroke width
const fixedStrokeWidth = 1; // Change this to your desired value

// Update the pen's stroke width for future drawings
await ea.viewUpdateScene({appState: {currentItemStrokeWidth: fixedStrokeWidth}});



